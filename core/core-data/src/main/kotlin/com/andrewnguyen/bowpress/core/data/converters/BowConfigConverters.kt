package com.andrewnguyen.bowpress.core.data.converters

import com.andrewnguyen.bowpress.core.database.entities.BowConfigEntity
import com.andrewnguyen.bowpress.core.model.BowConfiguration

fun BowConfigEntity.toDto(): BowConfiguration = BowConfiguration(
    id = id,
    bowId = bowId,
    createdAt = createdAt,
    label = label,
    drawLength = drawLength,
    letOffPct = letOffPct,
    peepHeight = peepHeight,
    dLoopLength = dLoopLength,
    topCableTwists = topCableTwists,
    bottomCableTwists = bottomCableTwists,
    mainStringTopTwists = mainStringTopTwists,
    mainStringBottomTwists = mainStringBottomTwists,
    topLimbTurns = topLimbTurns,
    bottomLimbTurns = bottomLimbTurns,
    restVertical = restVertical,
    restHorizontal = restHorizontal,
    restDepth = restDepth,
    sightPosition = sightPosition,
    sightPinDistance = sightPinDistance,
    gripAngle = gripAngle,
    nockingHeight = nockingHeight,
    specificGrip = specificGrip,
    specificLimbs = specificLimbs,
    frontStabWeight = frontStabWeight,
    frontStabAngle = frontStabAngle,
    rearStabSide = rearStabSide,
    rearStabWeight = rearStabWeight,
    rearStabVertAngle = rearStabVertAngle,
    rearStabHorizAngle = rearStabHorizAngle,
    braceHeight = braceHeight,
    tillerTop = tillerTop,
    tillerBottom = tillerBottom,
    plungerTension = plungerTension,
    clickerPosition = clickerPosition,
    rearStabLeftWeight = rearStabLeftWeight,
    rearStabRightWeight = rearStabRightWeight,
    isReference = isReference,
    referenceManuallyPinned = referenceManuallyPinned,
    avgArrowScore = avgArrowScore,
    scoreable = scoreable,
)

fun BowConfiguration.toEntity(pendingSync: Boolean = false): BowConfigEntity = BowConfigEntity(
    id = id,
    bowId = bowId,
    createdAt = createdAt,
    label = label,
    drawLength = drawLength,
    letOffPct = letOffPct,
    peepHeight = peepHeight,
    dLoopLength = dLoopLength,
    topCableTwists = topCableTwists,
    bottomCableTwists = bottomCableTwists,
    mainStringTopTwists = mainStringTopTwists,
    mainStringBottomTwists = mainStringBottomTwists,
    topLimbTurns = topLimbTurns,
    bottomLimbTurns = bottomLimbTurns,
    restVertical = restVertical,
    restHorizontal = restHorizontal,
    restDepth = restDepth,
    sightPosition = sightPosition,
    sightPinDistance = sightPinDistance,
    gripAngle = gripAngle,
    nockingHeight = nockingHeight,
    specificGrip = specificGrip,
    specificLimbs = specificLimbs,
    frontStabWeight = frontStabWeight,
    frontStabAngle = frontStabAngle,
    rearStabSide = rearStabSide,
    rearStabWeight = rearStabWeight,
    rearStabVertAngle = rearStabVertAngle,
    rearStabHorizAngle = rearStabHorizAngle,
    braceHeight = braceHeight,
    tillerTop = tillerTop,
    tillerBottom = tillerBottom,
    plungerTension = plungerTension,
    clickerPosition = clickerPosition,
    rearStabLeftWeight = rearStabLeftWeight,
    rearStabRightWeight = rearStabRightWeight,
    isReference = isReference ?: false,
    referenceManuallyPinned = referenceManuallyPinned ?: false,
    avgArrowScore = avgArrowScore,
    scoreable = scoreable ?: false,
    pendingSync = pendingSync,
)
